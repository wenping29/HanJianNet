name: PRD 版本打包

# 每次修改 PRD.md（即 PRD 版本变更）后，自动对 web / admin / mobileapp 三个项目进行打包构建；
# 也支持手动触发（workflow_dispatch）。
#
# 版本号从 PRD.md 顶部的「版本号：x.y.z」字段读取，用作打包产物名称。
# 构建产物通过 actions/upload-artifact 上传，可在工作流运行页面的 Artifacts 中下载。
# - web、admin   ：产出 dist/
# - mobileapp    ：Android 产出 APK；iOS 产出未签名 Runner.app（--no-codesign，zip 后上传，不出 .ipa）

on:
  push:
    branches:
      - "PRD"
      - "prd"
    paths:
      - "PRD.md"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  resolve-version:
    name: 读取并递增 PRD 版本号
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.v.outputs.version }}
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - id: v
        name: 读取版本号并自动递增小版本
        run: |
          VER=$(grep -E '版本号' PRD.md | head -n1 | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -n1)
          if [ -z "$VER" ]; then echo "::warning::PRD.md 未找到版本号，使用默认 0.0.0"; VER="0.0.0"; fi
          IFS='.' read -r MAJ MIN PATCH <<< "$VER"
          PATCH=$((PATCH + 1))
          NEW_VER="$MAJ.$MIN.$PATCH"
          echo "version=$NEW_VER" >> "$GITHUB_OUTPUT"
          echo "旧版本号: $VER -> 新版本号: $NEW_VER"
          echo "NEW_VER=$NEW_VER" >> "$GITHUB_ENV"

      - name: 写回新版本号到 PRD.md
        run: |
          LINE=$(grep -nE '版本号' PRD.md | head -n1 | cut -d: -f1)
          if [ -z "$LINE" ]; then echo "::error::未找到版本号行"; exit 1; fi
          sed -i "${LINE}s/[0-9]\+\.[0-9]\+\.[0-9]\+/$NEW_VER/" PRD.md
          grep -E '版本号' PRD.md | head -n1

      - name: 提交并推送新版本号
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add PRD.md
          git -c core.quotepath=false commit -m "chore: 版本号自动递增至 $NEW_VER [skip ci]" || echo "无需提交"
          git push

  build-web:
    name: 打包 Web
    needs: resolve-version
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: 安装依赖
        run: npm ci

      - name: 构建
        run: npm run build

      - name: 上传 Web 部署包
        uses: actions/upload-artifact@v4
        with:
          name: web-dist-v${{ needs.resolve-version.outputs.version }}
          path: web/dist
          if-no-files-found: error
          compression-level: 9

  build-admin:
    name: 打包 Admin 后台
    needs: resolve-version
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: admin
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: admin/package-lock.json

      - name: 安装依赖
        run: npm ci

      - name: 构建
        run: npm run build

      - name: 上传 Admin 部署包
        uses: actions/upload-artifact@v4
        with:
          name: admin-dist-v${{ needs.resolve-version.outputs.version }}
          path: admin/dist
          if-no-files-found: error
          compression-level: 9

  build-android:
    name: 打包 Android APK
    needs: resolve-version
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobileapp
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: 安装依赖
        run: flutter pub get

      - name: 构建 APK
        run: flutter build apk --release

      - name: 上传 Android APK
        uses: actions/upload-artifact@v4
        with:
          name: mobileapp-android-v${{ needs.resolve-version.outputs.version }}
          path: mobileapp/build/app/outputs/flutter-apk/*.apk
          if-no-files-found: error
          compression-level: 9

  build-ios:
    name: 打包 iOS（未签名）
    needs: resolve-version
    runs-on: macos-latest
    defaults:
      run:
        working-directory: mobileapp
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: 安装依赖
        run: flutter pub get

      - name: 构建 iOS（不签名，产物为 Runner.app）
        run: |
          flutter build ios --no-codesign
          cd build/ios/iphoneos && zip -q -r Runner.app.zip Runner.app

      - name: 上传 iOS Runner.app
        uses: actions/upload-artifact@v4
        with:
          name: mobileapp-ios-v${{ needs.resolve-version.outputs.version }}
          path: mobileapp/build/ios/iphoneos/Runner.app.zip
          if-no-files-found: error
          compression-level: 9
