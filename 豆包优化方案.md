# 一、开发优化（代码层面，后端 API + 前端 React）

| 优先级  | 优化内容                           | 说明                                                          | 状态                             |
| ---- |:------------------------------:| ----------------------------------------------------------- | ------------------------------ |
| 🔴紧急 | 关闭.NET 生产环境详细异常堆栈              | ASPNETCORE_ENVIRONMENT=Production，禁止向前端返回 SQL、代码堆栈信息        | ✅ 2026-09-18 |
| 🔴紧急 | 增加健康检查接口                       | /health    同时检测 MySQL、Redis 连通性，给监控系统拨测使用| ✅ 2026-09-18 |
| 🔴紧急 | API 增加熔断降级逻辑                   | Redis 不可用时自动降级直连 MySQL，防止缓存雪崩拖垮数据库| ✅ 2026-09-18                   |
| 🟡中期 | 分页改造：Offset 分页 → Keyset 游标分页   | 解决深度分页（800 + 页）MySQL 大 offset 性能灾难；保留原有页码兼容，大页码结果走 Redis 缓存 | ✅ 2026-09-18                   |
| 🟡中期 | 增加接口限流中间件（AspNetCoreRateLimit） | API 层二次限流，配合 Nginx 限流，双重防护爬虫批量抓取                            | ✅ 2026-09-18 |
| 🟡中期 | 完善 Redis 缓存逻辑                  | 首页统计、前 50 页列表、人物详情缓存；设置合理 TTL，缓存 key 设计，避免大 key             | ✅ 2026-09-18                   |
| 🟡中期 | 代码检查 DbContext 释放              | 确认所有查询使用 using，防止数据库连接泄漏、连接池耗尽；排查静态集合造成内存泄漏                 | ✅ 2026-09-18 |
| 🟢长期 | 后台管理增加操作日志                     | 档案新增 / 修改 / 删除操作留痕；支持单条档案快速屏蔽下架接口，应对内容投诉                    | ✅ 2026-09-18                   |
| 🟢长期 | 前端 React 静态资源增加 hash           | 打包 js/css 带上 contenthash，解决浏览器缓存旧页面问题；完善 404 兜底页面           | ✅ 2026-09-18 |

## 已完成事项说明

### ✅ API 熔断降级（2026-09-18 完成）

**问题**：原 `CacheService` 的"降级"逻辑存在致命缺陷——服务注册为 Scoped（每请求一个新实例），熔断计数和内存降级缓存都是请求级的，熔断永远不会触发；Redis 宕机时每个请求仍要等连接超时（秒级），会拖垮整个 API。

**方案**（改动文件：`webapi/src/Services/RedisCircuitBreaker.cs` 新增、`CacheService.cs` 重写内部实现、`Program.cs`、`Options/RedisOptions`）：

1. **单例熔断器 `RedisCircuitBreaker`**（三态状态机，跨请求共享状态）：
   - Closed → Redis 连续失败 3 次（`Redis:CircuitBreakerFailureThreshold` 可配）→ **Open**：30s（`Redis:CircuitBreakerOpenSeconds` 可配）内所有缓存读写快速跳过，读请求**直接回源 MySQL**，不再等超时；
   - 到期进入 **HalfOpen**：只放行 1 个探测请求，成功则恢复 Closed，失败重新熔断计时。
2. **防脏数据**：熔断期间写操作触发的缓存失效会丢失，熔断器记录"脏组"，恢复后由读/写路径自动补做整组失效。
3. **防击穿**：缓存未命中按 key 加 single-flight 锁，同一 key 并发回源只放 1 个请求查库。
4. **防雪崩**：缓存 TTL 加 ±10% 随机抖动，避免大量 key 同时过期。
5. **配套调优**：Redis 客户端 `ConnectTimeout/SyncTimeout=2s`、`AbortOnConnectFail=false`（Redis 宕机不阻塞应用启动）；启动日志中 Redis 连接串脱敏（原代码明文打印密码）；降级日志全部接入 Serilog。
6. 业务侧（TraitorService 等）零改动，公开 API 不变。

**验证**：以错误 Redis 地址启动，前 3 次请求触发熔断（日志 `Redis 熔断器打开`），其后请求约 0.5s 直连 MySQL 正常返回；30s 后日志可见半开探测与重新熔断；全程接口 200 无中断。

### ✅ 分页改造：Offset → Keyset 游标分页（2026-09-18 完成）

**方案**（改动：`TraitorService.cs`、`TraitorsController.cs`、`Common/PagedResult.cs`、`Program.cs`、`mobileapp` 三处、`web/src/lib/api.ts` 类型）：

1. **双模式分页**（`GET /api/traitors`，完全向后兼容）：
   - **页码模式**：传 `page/pageSize`，行为不变（返回 `total/totalPages`），web 前台页码跳转 UI 与旧版 App 无需修改；深页结果继续走 Redis 缓存（single-flight 防击穿），爬虫反复翻深页只打一次库；
   - **游标模式**：查询串带 `cursor` 键（空值=第一页），Keyset 谓词 `(HarmLevel, CreatedAt desc, Id)` 直接定位游标行之后，**不做 COUNT、不做大 OFFSET**，深翻页代价与页码无关；响应 `total=-1`，用 `nextCursor` 续翻，`null` 表示没有下一页；非法游标返回 400；
   - 页码模式响应也附 `nextCursor`，支持"页码跳转后转游标续翻"。
2. **排序链补 `Id` 终判**（GUID 无序但唯一），保证全序、跨页不重不漏。
3. **复合索引** `IX_Traitors_HarmLevel_CreatedAt (HarmLevel, CreatedAt)`：启动时经 `DbInitHelpers.EnsureIndexAsync` 幂等创建（InnoDB 二级索引自带主键，等效覆盖三列）。
4. **Flutter 端**无限滚动改用 `nextCursor` 驱动（home/search 两屏）；旧版 App 继续走 page 参数互不影响。
5. **顺带修复**：`DbInitHelpers.EnsureTableAsync` 用 `COUNT(*)+AnyAsync()` 判断表是否存在——`COUNT(*)` 恒返回一行导致判断恒真、缺表永远不建（存量 Bug），已改为取计数值判断（标量 SqlQuery 需 `AS Value` 别名）。
6. 管理端列表与日志查询保留 Offset（内部页、量可控），未改动；不传分页参数的全量返回（地图页）未改动。

**验证**（本地 SQLite 全新库 + 10 条含相同 CreatedAt、空 HarmLevel 的测试数据）：游标连续翻 3 页与 Offset 页码序列**完全一致、无重复**（含空 HarmLevel 跨组、同 CreatedAt 的 Id 终判跨页边界）；非法游标 400；`province` 过滤下游标翻页正常；全量分支回归通过；启动日志确认索引幂等创建。`dotnet build` 0 错误；`flutter analyze` 无 error。

**部署注意**：下次发布启动时会自动在生产 MySQL 的 `Traitors` 表执行 `CREATE INDEX IX_Traitors_HarmLevel_CreatedAt`（幂等，1 万行秒级完成）。



✅ 分页改造：首页统计、前 50 页列表、人物详情缓存；设置合理 TTL，缓存 key 设计，避免大 key（2026-09-18 完成）

改动摘要

| 文件| 改动|
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| webapi/src/Services/CacheService.cs| 修复双重前缀 Bug（线上 key 原是hanjian:hanjian:...）；新增 >32KB 值自动 gzip 透明压缩；暴露 StatsExpiry/ListExpiry/DetailExpiry/MaxCachedListPage |
| webapi/src/Services/TraitorServic.cs| 页码 >50 页、非空游标页绕过缓存直连MySQL；列表 key 过滤值 Trim + 超 200字符取 MD5；详情 60 分钟、统计三项 30 分钟TTL|
| webapi/src/Services/AtrocityCaseService.cs | 列表/详情接入分层 TTL|
| webapi/src/Options/Options.cs| 新增 5 个可配置项（三类TTL、最大缓存页码、压缩阈值）|
| webapi/appsettings.json| Redis:Enabled →true（生产缓存正式开启，熔断降级兜底）|
| 豆包优化方案.md| 标记 ✅ 并附实现说明|



  验证结果（本地 Redis + redis-cli 实测）

- key 形态：单层前缀 hanjian:traitors:v0:list:... ✓

- 分层 TTL（含 ±10% 抖动）：stats 1805s、list 552s、detail 3614s ✓

- 缓存收敛：请求第 51 页和非空游标页后 Redis 无新 key，绕行生效 ✓

- gzip 压缩：3240B JSON → 576B 存储（\x1f\x8b 开头，5.6
  倍），缓存读回与原响应逐字节一致 ✓

- dotnet build 0 错误；测试进程与本地 Redis 均已停止。
  部署提示：本次发布后缓存即真正生效。旧的双前缀 key（如有）已在 10
  分钟内自然过期，无需清理；建议发布后观察一段时间 Redis INFO memory
  与命中率，确认深页绕行达到预期效果。

### ✅ 关闭生产环境详细异常堆栈（2026-09-18 完成）

**结论先行**：代码层本就不泄露堆栈——全库无 `UseDeveloperExceptionPage`/`EnableSensitiveDataLogging`，`ExceptionHandlingMiddleware` 对未处理异常在任何环境都只返回固定的 `500 {"message":"服务器内部错误"}`，堆栈/SQL 只写 Serilog 文件与 ErrorLogs 表（内部留存排障，不进 HTTP 响应）；`ApiException`/`ArgumentException` 返回的均为手写中文业务消息。本次补齐三个真实缺口（改动：`webapi/Program.cs`、`deploy/hanjian-webapi.service` 新增、`deploy/pack.ps1` 注释）：

1. **Swagger 生产关闭**：`UseSwagger()/UseSwaggerUI()` 原无条件启用，生产环境暴露完整 API 文档（接口结构、参数、DTO schema）。现包进 `if (app.Environment.IsDevelopment())`，生产返回 404。
2. **`ASPNETCORE_ENVIRONMENT=Production` 落地**：新增 `deploy/hanjian-webapi.service`（systemd 单元模板），显式声明 `Environment=ASPNETCORE_ENVIRONMENT=Production`，并带 `Restart=always`/`RestartSec=5` 崩溃自动拉起；`pack.ps1` 顶部注释指向该模板（服务器上拷贝至 `/etc/systemd/system/` 后 `daemon-reload` 启用）。
3. **中间件顺序加固**：`ExceptionHandlingMiddleware` 上移到管道最前（Serilog 请求日志之后、请求体缓冲/Crypto/审计之前），保证其后所有中间件（含 Crypto 加密层）抛出的异常都被统一兜底并写错误日志；此前 Crypto 层自身异常会绕过全局处理（不泄露堆栈但不留日志）。加密客户端兼容已确认：web/admin 前端仅在响应带 `X-Encrypted: 1` 头时解密，否则按明文解析，明文错误 JSON 正常处理。

**验证**（本地临时 SQLite 库，`--no-launch-profile` 避免 launchSettings 覆盖环境变量）：

- Development：`/swagger/index.html`、`/swagger/v1/swagger.json` 返回 200；列表/统计接口 200 ✓
- Production：两个 Swagger 地址均 404；列表/统计接口 200 ✓
- Production 下运行时删表制造未处理异常：响应为 `500 {"message":"服务器内部错误"}`，无堆栈/SQL；完整堆栈写入 `logs/hanjian-*.log`（内部留存），请求日志记录 `GET /api/atrocity-events -> 500` ✓
- `dotnet build` 0 错误；测试进程与临时库已清理。

**部署注意**：服务器若是用 systemd 托管，请按 `deploy/hanjian-webapi.service` 模板核对现有单元是否已设置 `ASPNETCORE_ENVIRONMENT=Production`；未设置时 .NET 默认即 Production，本次代码改动后 Swagger 在任何非 Development 环境都会关闭。

### ✅ 健康检查接口 /health（2026-09-18 完成）

**方案**（改动：`webapi/src/Controllers/HealthController.cs` 新增、`RedisCircuitBreaker.cs` 加只读状态属性）：

1. **双地址**：`GET /health`（任务要求）+ `GET /api/health`（兼容经 Nginx `/api` 前缀反代的拨测），均无需登录。
2. **双探测**（各 3s 超时，互不影响）：
   - **MySQL**：`CanConnectAsync` 真实建连验证（生产 MySQL 为真实连接；SQLite 下为文件可达性检查）；
   - **Redis**：绕过熔断器与 CacheService 的降级吞异常，直接对底层 `IDistributedCache` 做一次 GET 往返，如实反映连通性；配置未启用时返回 `disabled` 而非故障，便于监控区分"主动关闭"与"宕机"。
3. **分级结论**（HTTP 状态码供拨测直接判定）：
   - MySQL 不可用 → **503 `unhealthy`**（核心依赖，监控应立即告警）；
   - 仅 Redis 不可用 → **200 `degraded`**（缓存层有熔断降级、业务仍可用，监控可低级告警）；
   - 全部正常 → **200 `healthy`**。
4. **响应体**：`{ status, timestamp, checks: { mysql: { status, latencyMs, error, provider }, redis: { status, latencyMs, error, circuitBreaker } } }`；error 只给异常类型名/timeout，不含连接串等细节（端点公开，避免泄露基础设施信息）；`circuitBreaker` 附带熔断器实时状态（closed/open/halfopen）。

**验证**（本地临时 SQLite 库 + 本地 Redis 6399，Production 环境）：

- MySQL + Redis 均通：`200 healthy`，mysql 20ms / redis 69ms，`circuitBreaker: closed` ✓
- 停掉 Redis 后：`200 degraded`，redis `unhealthy`（RedisConnectionException，单次探测 ~2.9s 被 3s 超时要约）✓
- Redis 配置关闭：`200 healthy`，redis `disabled` ✓
- MySQL 故障 → 503 分支：控制流为 `CanConnect=false → unhealthy → 503` 直白逻辑，本地 Windows 文件锁无法运行时模拟 MySQL 断连，经代码走查确认；下次发布后可临时停库实测。
- `/api/health` 同样 200 ✓；`dotnet build` 0 错误；测试进程与临时库已清理。

**部署/监控提示**：拨测频率建议 30~60s；每次拨测会写一条 Serilog 请求日志（`GET /health -> 200`），如嫌吵可在 Serilog 配置中按路径过滤，或等"监控告警"专项统一处理。Nginx 若只反代 `/api` 前缀，监控请拨 `http://<域名或IP>/api/health`。

### ✅ 接口限流中间件 AspNetCoreRateLimit（2026-09-18 完成）

**方案**（改动：`webapi/HanJianNet.WebApi.csproj` 加包、`Program.cs`、`appsettings.json` 新增 `IpRateLimiting` 节）：

1. **AspNetCoreRateLimit 5.0.0**，IP 维度限流、进程内存计数器（单实例部署足够；将来多实例再换分布式计数存储）。中间件置于全局异常处理之后、请求体缓冲/解密之前——超限请求 429 直接短路，不消耗后续资源。
2. **分级规则**（`EnableEndpointRateLimiting=true`，同时命中的规则取最严）：
   - `get:/api/traitors`（列表/搜索/地图全量）60 次/分——正常浏览约 1 次/秒足够，爬虫翻 899 页至少 15 分钟；
   - `get:/api/traitors/*`（详情）120 次/分；
   - `post:/api/auth/login`、`post:/api/auth/register` 各 10 次/分（顺带防暴力破解）；
   - 全局兜底 `*`：20 次/秒 + 300 次/分（覆盖后台、上传等其余接口）；
   - 正常响应带 `X-Rate-Limit-Limit/Remaining/Reset` 头，429 响应带 `Retry-After`。
3. **真实 IP 识别**：经 `X-Real-IP` 头取客户端真实 IP（Nginx 反代后 RemoteIpAddress 是 127.0.0.1），不同访客计数器互不影响。
4. **429 响应**统一为中文 JSON `{"message":"请求过于频繁，请稍后再试"}`，与全局异常格式一致。
5. **踩坑**：`QuotaExceededResponse.Content` 会被库内部 `string.Format` 处理，JSON 花括号必须写成 `{{ }}` 转义，否则超限时抛 `FormatException` 变 500（测试中发现并修复）。

**验证**（本地临时 SQLite 库，Production 环境）：

- 列表规则调为 2 次/分：第 3、4 次请求返回 `429 {"message":"请求过于频繁，请稍后再试"}` + `Retry-After: 60` ✓
- 同一端点换 `X-Real-IP` 头 → 计数器独立，仍 200 ✓；被限流时其他端点（stats、/health）不受影响 ✓
- 默认配置：列表响应带 `X-Rate-Limit-Remaining: 59`；连续 10 次 stats、/health 均 200 ✓
- `dotnet build` 0 错误；测试进程与临时库已清理。

**部署注意**：① Nginx 站点配置需有 `proxy_set_header X-Real-IP $remote_addr;`（常见配置已带），否则所有访客被识别为同一 IP（127.0.0.1）互相挤占额度；② 限流计数是进程内存，应用重启后清零；③ 若后续按安全组专项收紧到仅 80 端口对外开放，X-Real-IP 只能由 Nginx 写入，访客无法伪造；在 3000 端口仍可达期间，直连者理论上可伪造该头绕过限流（Nginx 层限流不受此影响）。

### ✅ DbContext 释放与内存泄漏排查（2026-09-18 完成）

**审查结论：未发现连接泄漏或静态集合泄漏**，DbContext 生命周期管理本就规范——

1. **DbContext 释放** ✓
   - 全库无 `new AppDbContext()`：所有用点（14 个服务 + 3 个控制器）均为构造函数注入；`AddDbContext` 默认 Scoped 生命周期，请求结束由 DI 容器自动 Dispose，连接归还连接池；
   - 启动种子数据在 `using (var scope = app.Services.CreateScope())` 内执行，作用域随 `using` 释放；
   - 无 `IDbContextFactory`/后台线程/`Task.Run` 持库场景（无 BackgroundService、无 Timer），不存在"请求结束后继续使用已释放 DbContext"的风险；
   - 3 个单例（`RedisCircuitBreaker`、`CryptoService`、限流配置）均不依赖 AppDbContext，无"单例捕获作用域依赖"的 captive-dependency 问题；
   - 审计过滤器、异常中间件的日志写库全部 `await`（无 fire-and-forget 丢弃任务），两处 `_ = await` 仅为忽略返回值的已等待调用。
2. **静态集合** ✓
   - `CacheService.KeyLocks`（single-flight 锁字典）：条目在锁释放且空闲时 `TryRemove` 清理，规模受并发数约束，不会无限增长；
   - `AuditActionFilter.SkipPrefixes/LoginPathEndings`、`ProvinceMatcher` 两个字典：均为初始化后只读的固定集合；
   - 限流计数器（AspNetCoreRateLimit MemoryCache）带过期自动清理。
3. **非托管资源** ✓：文件流/StreamReader 全部 `using`/`await using`；HttpClient 经 `IHttpClientFactory` 创建（AiService、UploadService），无 socket 耗尽风险。

**顺手优化**（改动：`TraitorService.cs` 8 处、`AtrocityCaseService.cs` 2 处）：热路径只读查询补 `AsNoTracking()`——公开列表（Offset+游标+地图全量共用的 `ApplyListFilters`）、公开详情、修订列表、后台列表、批量导出、查重、事件列表/详情、创建后回读。这些查询返回实体后仅映射 DTO 不再写库，不跟踪可省去 ChangeTracker 快照，降低每请求内存与 CPU；更新/删除/合并路径保持跟踪不动（标量/匿名投影与 DTO 投影本就不跟踪，未改）。

**回归验证**（本地临时 SQLite 库）：公开列表（页码+游标）、详情（含 Includes 嵌套数据完整）、统计、分省统计、后台列表、批量导出、查重、事件列表/详情全部 200；**更新路径（依赖跟踪）实测写入生效**；`dotnet build` 0 错误；测试进程与临时库已清理。

### ✅ 前端静态资源 contenthash + 404 兜底（2026-09-18 完成）

**排查结论**：两项需求的代码基础本就存在——Vite 默认产物即 `assets/[name]-[hash].js/css`（内容哈希）；web、admin 两端均已有 React Router `path="*"` 兜底路由与 NotFound 页面（i18n 键经 8 种语言逐一核对无缺失）。本次做显式化与强化（改动：`web/vite.config.ts`、`admin/vite.config.ts`）：

1. **显式 contenthash 命名**：`entryFileNames/chunkFileNames/assetFileNames` 写进配置（不再依赖 Vite 默认行为，防升级漂移）。内容不变文件名不变（可放心长缓存）；内容变化即换名，`index.html` 引用随之更新，浏览器必然拉新——"缓存旧页面"问题的根治前提。
2. **vendor 分包**：`react/react-dom/react-router-dom` 单独成 chunk（web 165KB、admin 类似），业务代码更新不再波及框架大依赖的缓存，减少发布后重复下载。
3. **404 兜底**（维持现状，已验证完整）：web 端水墨风 404 页（标题+说明+返回首页），admin 端 404 页（返回审核列表）；深链接与不存在的路由由 SPA 回退到 `index.html` 后前端路由渲染 404。

**验证**：

- 两端 `npm run build` 成功；产物 `index-<hash>.js/css` + `vendor-<hash>.js`，`index.html` 引用全部带 hash ✓
- `vite preview` 实测：深链接 `/traitor/xxx` 与未知路径 `/no-such-page` 均回退 SPA（200，返回 index.html）；带 hash 资源 200 ✓

**部署注意（Nginx 侧，属"Nginx 静态资源缓存策略"专项的前置要求）**：hash 机制要生效，Nginx 必须满足两条——

```nginx
location = /index.html { add_header Cache-Control "no-cache"; }        # 入口 HTML 不长缓存，否则引用的 hash 文件名永远不更新
location /assets/ { try_files $uri =404; expires 1y; add_header Cache-Control "public, immutable"; }  # 带 hash 资源长缓存
location / { try_files $uri /index.html; }                             # SPA 路由回退；/assets/ 单独 =404 防止已删除旧 hash 资源被回退成 HTML
```

另：`web/public/` 下的静态文件（如地图 `/data/`）按 Vite 约定原样拷贝、不带 hash，更新后需靠 Nginx 对该路径设置短缓存或协商缓存。

### ✅ 后台操作日志留痕 + 单条档案快速下架（2026-09-18 完成）

**问题**：操作日志的通用框架（`OperationLog` 实体、`AuditActionFilter`、`LogService`、后台「操作日志」页）此前已就位，但日志行的「对象描述」`TargetLabel` 恒为空——只能看到记录 Id，无法直接看出改的是哪条档案；同时缺「单条档案快速下架」能力，出现内容投诉时无法立即屏蔽对应条目。

**方案**（改动：`webapi/src/Entities/Traitor.cs`、`Program.cs`、`Dtos/TraitorDtos.cs`、`Dtos/Mappings.cs`、`Services/TraitorService.cs`、`Services/LogService.cs`、`Filters/AuditActionFilter.cs`、`Controllers/TraitorsController.cs`；前端 `admin/src/pages/Traitors.tsx`、`lib/api.ts`、`types/index.ts`、`i18n/zh|en.json`）：

1. **操作日志留痕**：新增 `LogService.ResolveTraitorLabelAsync`——优先从请求体 JSON 的 `name` 取（新增/修改），否则按 Id 查库（删除/下架等无 body 场景）；`AuditActionFilter` 对 `module == "traitors"` 的写操作补全 `TargetLabel`，并给下架端点映射动作名 `set-status`。日志行现在直接可读为「汪精卫 · 档案已下架」。
2. **下架数据模型**：`Traitor` 新增 `IsHidden`（bool）、`HiddenReason`、`HiddenAt`、`HiddenBy` 四字段，留痕「谁在何时因何下架」。
3. **下架接口**：`PATCH /api/admin/traitors/{id}/status`（`[Authorize(admin,superadmin)]`），请求体 `{ hidden, reason }`；下架时记录原因/时间/操作人，恢复时清空。
4. **前台全面屏蔽**：`TraitorService` 公开读取全路径过滤 `!IsHidden`——列表（Offset + Keyset 游标共用的 `ApplyListFilters`）、详情（返回 404）、修订历史、统计、分省统计、时间轴；下架/恢复后 `cache.InvalidateAsync(CacheGroup)` 整组失效，改动即时生效。管理员列表/详情不过滤，便于查看与恢复。
5. **增量建列**：`Program.cs` 的 `DbInitHelpers.EnsureColumnAsync` 补 4 条 ALTER（SQLite + MySQL 双方言），启动时幂等执行，无需手工迁移。
6. **前端**：名录列表每行加「下架/恢复」按钮与「已下架」徽标（徽标带下架原因 tooltip），下架走确认弹窗并支持填写原因（选填）；类型、API 客户端、中英文案同步补齐。

**验证**：`dotnet build` 0 错误；admin 前端 `tsc --noEmit` 0 错误。

**部署注意**：下次发布启动时会自动在生产 MySQL 的 `Traitors` 表执行 4 条 `ALTER TABLE ADD COLUMN`（幂等，秒级完成）；其余语言（de/es/fr/ja/ko/ru）文案按既有 `fallbackLng: 'zh'` 回退中文，无需单独补译。

# 二、运维优化（服务、监控、发布、巡检、故障预案）

| 优先级  | 优化内容                    | 说明                                                          | 状态  |
| ---- | ----------------------- | ----------------------------------------------------------- | --- |
| 🔴紧急 | 配置.NET systemd 自动重启     | 进程崩溃自动拉起；配置内存上限，避免 OOM 吞噬整机资源                               |     |
| 🔴紧急 | 部署 Nginx 日志轮转 logrotate | 防止访问 / 错误日志无限膨胀占满磁盘                                         |     |
| 🔴紧急 | 搭建基础监控告警                | Prometheus+Grafana 采集服务器、.NET、MySQL、Redis 指标；告警渠道：企业微信 / 钉钉 |     |
| 🔴紧急 | 建立发布 + 回滚 SOP           | 禁止线上直接改代码；每次发布前备份数据库与版本包；保留上一版程序包用于一键回滚                     |     |
| 🟡中期 | 完善日常巡检台账                | 每日 / 每周 / 每月巡检清单，故障记录台账，故障复盘机制                              |     |
| 🟡中期 | 监控大盘重点观测指标              | 5xx 错误数、API 响应耗时、Redis 命中率、MySQL 慢查询、磁盘使用率                  |     |
| 🟡中期 | 维护运维手册                  | 记录服务启停命令、备份脚本路径、故障 SOP、账号信息                                 |     |
| 🟢长期 | 高可用架构改造                 | MySQL 主从、Redis 主从哨兵、.NET 多实例 + Nginx 负载均衡，消除单点故障            |     |

# 三、备份优化（数据资产保护，最高优先级）

| 优先级  | 优化内容                     | 说明                                               | 状态  |
| ---- | ------------------------ | ------------------------------------------------ | --- |
| 🔴紧急 | 启用 MySQL 全量备份脚本 + binlog | 每日凌晨 mysqldump 全量备份，开启 binlog 实现时间点恢复，RPO 压缩至分钟级 |     |
| 🔴紧急 | 备份文件异地同步                 | 备份文件不保留在本机，自动上传至对象存储 / 备用服务器，防止服务器硬盘损坏丢失全部数据     |     |
| 🔴紧急 | Redis RDB 定时备份脚本         | 定时拷贝 dump.rdb 并异地存储；缓存内业务临时数据可保留                 |     |
| 🟡中期 | 每周执行备份恢复演练               | 导入备份库，校验档案总记录数量，调用 API 验证查询，确认备份包可用（很多备份包会损坏）    |     |
| 🟡中期 | 备份生命周期策略                 | 日备份保留 7 天，周备份保留 3 个月，月度归档永久保存                    |     |
| 🟡中期 | 备份任务监控告警                 | 脚本执行失败触发告警，避免备份任务静默失败而无人发现                       |     |

# 四、安全加固（服务器、Nginx、数据库、网络）

| 优先级  | 优化内容                   | 说明                                                            | 状态  |
| ---- | ---------------------- | ------------------------------------------------------------- | --- |
| 🔴紧急 | 云安全组 / 防火墙收紧           | 仅开放 80 端口；SSH、MySQL、Redis 端口禁止公网访问，仅允许运维指定 IP 访问 SSH          |     |
| 🔴紧急 | Nginx                  | 配置 API 限流 + 隐藏 Nginx 版本号    限制单 IP 请求频率，拦截爬虫；隐藏中间件指纹，减少扫描探测信息 |     |
| 🔴紧急 | 数据库账号最小权限原则            | 程序连接 MySQL 使用专用账号，禁止 root 账号连接业务；Redis 设置密码，禁止公网暴露 Redis 端口   |     |
| 🔴紧急 | SSH 安全加固               | 禁止 root 登录 SSH，使用普通账号 + 密钥登录，关闭密码登录；强密码策略                     |     |
| 🟡中期 | 配置 robots.txt，限制爬虫抓取范围 | 约束爬虫，禁止一次性抓取全部 899 页档案，降低数据库压力                                |     |
| 🟡中期 | 访问日志留存≥6 个月            | Nginx 访问日志留存，用于攻击溯源、异常访问排查                                    |     |
| 🟡中期 | 绑定域名 + 部署 HTTPS 证书     | 不再裸 IP HTTP 明文访问，规避传输窃听风险                                     |     |
| 🟡中期 | 接入云 WAF    拦截          | SQL 注入、XSS、端口扫描、CC 爬虫攻击，把恶意流量挡在服务器外部                          |     |
| 🟢长期 | 定期漏洞扫描                 | 每月扫描服务器、中间件、Nuget 依赖包漏洞，及时修复高危漏洞                              |     |

# 五、性能优化（数据库、缓存、接口响应）

| 优先级  | 优化内容                                               | 说明                                      | 状态  |
| ---- | -------------------------------------------------- | --------------------------------------- | --- |
| 🔴紧急 | MySQL 建立联合索引                                       | 针对朝代、时期筛选字段建立联合索引，避免多条件查询触发全表扫描         |     |
| 🔴紧急 | Redis 配置内存淘汰策略allkeys-lru                          | 防止 Redis 内存占满导致服务异常                     |     |
| 🟡中期 | 开启 MySQL 慢查询日志                                     | 抓取慢 SQL，持续优化分页、筛选查询语句                   |     |
| 🟡中期 | 大 key 清理，拆分 Redis 缓存 key                           | 避免超大 key 造成 Redis 卡顿                    |     |
| 🟡中期 | Nginx 静态资源缓存策略                                     | js/css/ 图片设置合理 Cache-Control，减少静态资源重复请求 |     |
| 🟢长期 | 静态资源 CDN 加速    前端 React 打包静态资源放 CDN，减轻服务器 Nginx 压力 |                                         |     |

# 六、业务专项优化（内容、合规、业务应急）

| 优先级  | 优化内容         | 说明                        | 状态  |
| ---- | ------------ | ------------------------- | --- |
| 🔴紧急 | 准备单条档案快速下线能力 | 出现投诉舆情时，可快速屏蔽对应档案条目       | ✅ 2026-09-18 |
| 🟡中期 | 内容定期抽样巡检     | 抽查页面档案、链接，防止数据迁移更新后页面内容异常 |     |
| 🟡中期 | 业务访问统计监控     | 监控访客量，突发流量预警，提前应对访问暴涨     |     |
| 🟢长期 | 内容合规自查机制     | 定期对档案内容做自查，降低投诉风险         |     |
